$VERBOSE_BUILD = true
$COMPILER_PREFIX = "ccache"
$COMPILER = "clang++-3.8"
$COMPILE_FLAGS = "-std=c++1y -pthread -Wall -I. -mfpu=neon"
#$ADDITIONAL_LINK_FLAGS = "-L/usr/local/lib -ltcmalloc_and_profiler"
task :default => "release/ntest.exe"
